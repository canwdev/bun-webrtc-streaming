root = "."
tmp_dir = "tmp"

[build]
  cmd = "cmd /c \"bun run copy:frontend && go build -o ./tmp/webrtc-server.exe .\""
  bin = "./tmp/webrtc-server.exe"
  include_ext = ["go", "html"]
  exclude_dir = ["bin", "tmp", "assets"]
  delay = 500
  stop_on_error = true
